/**
 * Mensaje genérico de recuperación de contraseña.
 *
 * Se usa tanto en éxito como en error para prevenir la enumeración de usuarios:
 * la UI no debe revelar si el correo existe o no.
 */
export const FORGOT_PASSWORD_GENERIC_MESSAGE =
    'Si el correo existe, se enviará un enlace de recuperación.';
